[project]
name = "build-chalaoshi"
version = "0.1.0"
description = "查老师 - chalaoshi 离线网站构建工具"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "requests>=2.28.0",
    "pyyaml>=6.0",
    "urllib3<2.0",
]

[tool.uv]
package = false
